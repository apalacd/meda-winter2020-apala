let arguments = process.argv;

console.log(arguments);

let number1 = arguments[2];
/*let number2 = arguments[3]; */

letnumber2 = argument[4];

let num1Converted = parseInt(number1);
let num2Converted = parseInt(number2);

if (isNaN(num1Converted) || isNaN(num2Converted)) {
    console.log("Error 349923: The first or second numner is not an actual number!");
} else if (mathOperator !== "") {
    
} else {

    if (mathOperator === "+") {
        condole.log('The sum of of ${num1Converted} and ${num2Converted} is ${num1Converted + num2Converted} .')
        is ${num1Converted + num2Converted} is ${num1Converted }}
    
        console.log('The sum of ${num1Converted} and ${num2Converted} is ${num1Converted + num2Converted} .');
}

console.log(typeof num1Converted, typeof num2Converted);
} 

// console.log(typeof num1Convrted, typeof num2Converted);)